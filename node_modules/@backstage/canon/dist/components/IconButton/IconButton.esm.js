import { jsx } from 'react/jsx-runtime';
import { forwardRef } from 'react';
import '@remixicon/react';
import { Icon } from '../Icon/Icon.esm.js';
import '../Icon/context.esm.js';
import clsx from 'clsx';
import { useResponsiveValue } from '../../hooks/useResponsiveValue.esm.js';

const IconButton = forwardRef(
  (props, ref) => {
    const {
      size = "medium",
      variant = "primary",
      icon,
      className,
      style,
      ...rest
    } = props;
    const responsiveSize = useResponsiveValue(size);
    const responsiveVariant = useResponsiveValue(variant);
    return /* @__PURE__ */ jsx(
      "button",
      {
        ref,
        className: clsx("canon-IconButton", className),
        "data-size": responsiveSize,
        "data-variant": responsiveVariant,
        style,
        ...rest,
        children: /* @__PURE__ */ jsx(Icon, { name: icon, className: "canon-IconButtonIcon" })
      }
    );
  }
);

export { IconButton, IconButton as default };
//# sourceMappingURL=IconButton.esm.js.map
